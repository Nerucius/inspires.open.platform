import toastr from "toastr";
import "toastr/build/toastr.min.css";

toastr.options.closeButton = true;
toastr.options.positionClass = "toast-top-center"

toastr.options.toastClass = "v-snackbar"

toastr.options.iconClasses = {
  // error: 'error',
  // info: 'info',
  // success: 'success',
  // warning: 'warning'
}